<div class="text-center">
  <h1>{{name}}</h1>
  <P>{{description}}</P>

  <p>Posted by {{user.name}} at {{format_date date_created}}</p>

  <p>Posted by {{user.name}} at {{format_date date_created}}</p>

  <p>Posted by {{user.name}} at {{format_date date_created}}</p>

    <div class="d-grid gap-2">
    <button class="btn btn-primary" type="button">Homepage</button>
  </div>

</div>